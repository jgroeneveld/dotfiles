" removing janus mappings
:unmap <leader>tw

" disable nerdtree from opening on start (janus)
augroup AuNERDTreeCmd
  autocmd!
augroup end


" load all files in vimrc.d
for f in split(glob('~/dotfiles/vimrc.d/*.vim'), '\n')
  exe 'source' f
endfor

